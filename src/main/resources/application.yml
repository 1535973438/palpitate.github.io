spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yupi?serverTimezone=GMT%2B8v&serverTimezone=UTC&useSSL=false&rewriteBatchedStatements=true
    username: root
    password: 800165
  application:
    name: user-center
server:
  port: 8080

